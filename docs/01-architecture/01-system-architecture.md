# IronForge V2 - 系统架构设计

> 📅 创建日期: 2025-11-25  
> 🏗️ 版本: 2.0  
> 🎯 状态: 设计阶段

---

## 📋 目录

- [架构概览](#架构概览)
- [分层架构](#分层架构)
- [模块设计](#模块设计)
- [数据流设计](#数据流设计)
- [状态管理](#状态管理)
- [依赖关系](#依赖关系)
- [部署架构](#部署架构)

---

## 🏗️ 架构概览

### 架构定位：非托管钱包 + 企业级 API

**核心特点**：
- 🔑 **私钥本地存储** - 前端浏览器 IndexedDB 加密存储，用户完全掌控
- 🏢 **元数据云端管理** - 后端 API 管理钱包列表、交易历史、配置等
- 🔒 **零信任架构** - 后端永远不接触私钥和助记词
- 📊 **企业级服务** - 提供聚合查询、分析统计、审计日志等增值服务

### 架构原则

1. **分层清晰** - 严格的层次划分，单向依赖
2. **高内聚低耦合** - 模块职责明确，接口简洁
3. **依赖倒置** - 依赖抽象而非具体实现
4. **数据分离** - 敏感数据前端，元数据后端
5. **可测试性** - 每层都可独立测试
6. **可扩展性** - 插件化架构

### 整体架构图（非托管 + API 混合模式）

```
┌────────────────────────────────────────────────────────────────────┐
│                      Browser Environment (客户端)                   │
├────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │            Presentation Layer (UI)                            │  │
│  │  ┌──────────┬──────────┬──────────┬──────────┐             │  │
│  │  │  Pages   │Components│  Hooks   │  Styles  │             │  │
│  │  └──────────┴──────────┴──────────┴──────────┘             │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                            ↕ (Signal/Props)                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │         Application Layer (Business Logic)                   │  │
│  │  ┌──────────┬──────────┬──────────┬──────────┐             │  │
│  │  │ Services │  State   │  Actions │ Adapters │             │  │
│  │  └──────────┴──────────┴──────────┴──────────┘             │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                            ↕ (Interface)                       │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              Domain Layer (Core Logic)                   │  │
│  │  ┌──────────┬──────────┬──────────┬──────────┐         │  │
│  │  │ Entities │  Values  │  Rules   │ Contracts│         │  │
│  │  └──────────┴──────────┴──────────┴──────────┘         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                            ↕ (Trait)                           │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │          Infrastructure Layer (External)                │  │
│  │  ┌──────────┬──────────┬──────────┬──────────┐         │  │
│  │  │   API    │ Storage  │  Crypto  │  Utils   │         │  │
│  │  └──────────┴──────────┴──────────┴──────────┘         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
└───────────────────────────────────────────────────────────────┘
                              ↕ HTTP/RPC
┌───────────────────────────────────────────────────────────────┐
│                      External Services                          │
│  ┌──────────┬──────────┬──────────┬──────────┐               │
│  │ Backend  │Blockchain│  IPFS    │Third-Party│               │
│  │   API    │   RPC    │  Gateway │   APIs    │               │
│  └──────────┴──────────┴──────────┴──────────┘               │
└───────────────────────────────────────────────────────────────┘
```

---

## 📚 分层架构

### 1️⃣ Presentation Layer (表示层)

**职责**: UI 渲染、用户交互、路由管理

```rust
// 目录结构
src/presentation/
├── pages/              // 页面组件
│   ├── home/
│   │   ├── mod.rs     // 主页逻辑
│   │   └── styles.rs  // 主页样式
│   ├── wallet/
│   │   ├── list.rs
│   │   ├── detail.rs
│   │   └── create.rs
│   └── ...
├── components/         // 可复用组件
│   ├── common/        // 通用组件
│   │   ├── button.rs
│   │   ├── input.rs
│   │   ├── modal.rs
│   │   └── ...
│   ├── wallet/        // 业务组件
│   │   ├── wallet_card.rs
│   │   ├── balance_display.rs
│   │   └── ...
│   └── layout/        // 布局组件
│       ├── navbar.rs
│       ├── sidebar.rs
│       └── footer.rs
├── hooks/              // 自定义 Hooks
│   ├── use_wallet.rs
│   ├── use_auth.rs
│   ├── use_transaction.rs
│   └── ...
├── router/             // 路由配置
│   ├── mod.rs
│   ├── routes.rs
│   └── guards.rs
└── styles/             // 全局样式
    ├── theme.rs
    ├── tokens.rs
    └── animations.rs
```

**核心原则**:
- ✅ 只负责 UI 呈现和用户交互
- ✅ 不包含业务逻辑
- ✅ 通过 Signals/Props 与应用层通信
- ✅ 组件保持纯函数式（无副作用）

---

### 2️⃣ Application Layer (应用层)

**职责**: 业务逻辑、状态管理、事件处理

```rust
src/application/
├── services/           // 业务服务
│   ├── wallet_service.rs     // 钱包管理
│   ├── auth_service.rs       // 认证服务
│   ├── transaction_service.rs// 交易服务
│   ├── token_service.rs      // 代币服务
│   └── ...
├── state/              // 全局状态
│   ├── mod.rs
│   ├── wallet_state.rs
│   ├── auth_state.rs
│   ├── ui_state.rs
│   └── app_config.rs
├── actions/            // 用户操作
│   ├── wallet_actions.rs
│   ├── transaction_actions.rs
│   └── ...
├── adapters/           // 外部适配器
│   ├── api_adapter.rs
│   ├── storage_adapter.rs
│   └── crypto_adapter.rs
├── dto/                // 数据传输对象
│   ├── wallet_dto.rs
│   ├── transaction_dto.rs
│   └── ...
└── errors/             // 业务错误
    ├── mod.rs
    ├── wallet_error.rs
    └── transaction_error.rs
```

**核心原则**:
- ✅ 协调领域层和基础设施层
- ✅ 处理用户操作和业务流程
- ✅ 管理应用状态
- ✅ 不直接依赖外部实现

---

### 3️⃣ Domain Layer (领域层)

**职责**: 核心业务实体、值对象、业务规则

```rust
src/domain/
├── entities/           // 实体
│   ├── wallet.rs
│   ├── transaction.rs
│   ├── token.rs
│   ├── account.rs
│   └── ...
├── value_objects/      // 值对象
│   ├── address.rs
│   ├── amount.rs
│   ├── chain_id.rs
│   ├── mnemonic.rs
│   └── ...
├── rules/              // 业务规则
│   ├── validation.rs
│   ├── authorization.rs
│   └── ...
├── contracts/          // 接口定义（Traits）
│   ├── repositories.rs    // 仓储接口
│   ├── services.rs        // 服务接口
│   └── gateways.rs        // 网关接口
└── events/             // 领域事件
    ├── wallet_events.rs
    ├── transaction_events.rs
    └── ...
```

**核心原则**:
- ✅ 核心业务逻辑，不依赖外层
- ✅ 定义接口（Traits）供外层实现
- ✅ 包含业务验证规则
- ✅ 领域模型尽量保持不可变

---

### 4️⃣ Infrastructure Layer (基础设施层)

**职责**: 外部依赖、第三方集成、底层实现

```rust
src/infrastructure/
├── api/                // API 客户端
│   ├── client.rs
│   ├── endpoints.rs
│   ├── interceptors.rs
│   └── error_handler.rs
├── storage/            // 存储实现
│   ├── local_storage.rs
│   ├── indexed_db.rs
│   ├── secure_storage.rs
│   └── cache.rs
├── crypto/             // 加密实现
│   ├── aes.rs
│   ├── hash.rs
│   ├── signature.rs
│   └── key_derivation.rs
├── blockchain/         // 区块链客户端
│   ├── ethereum.rs
│   ├── bitcoin.rs
│   ├── ton.rs
│   └── ...
├── repositories/       // 仓储实现
│   ├── wallet_repository.rs
│   ├── transaction_repository.rs
│   └── ...
└── utils/              // 工具函数
    ├── logger.rs
    ├── formatter.rs
    ├── validator.rs
    └── ...
```

**核心原则**:
- ✅ 实现领域层定义的接口
- ✅ 处理与外部系统的交互
- ✅ 可随时替换实现
- ✅ 隔离外部依赖的变化

---

## 🔄 数据流设计

### 单向数据流

```
┌────────────────────────────────────────────────────────┐
│                     User Action                         │
│                    (Button Click)                       │
└──────────────────────┬─────────────────────────────────┘
                       ↓
┌────────────────────────────────────────────────────────┐
│                  Event Handler                          │
│              (Presentation Layer)                       │
└──────────────────────┬─────────────────────────────────┘
                       ↓
┌────────────────────────────────────────────────────────┐
│                 Dispatch Action                         │
│               (Application Layer)                       │
└──────────────────────┬─────────────────────────────────┘
                       ↓
┌────────────────────────────────────────────────────────┐
│              Execute Business Logic                     │
│                  (Domain Layer)                         │
└──────────────────────┬─────────────────────────────────┘
                       ↓
┌────────────────────────────────────────────────────────┐
│              Call External Service                      │
│             (Infrastructure Layer)                      │
└──────────────────────┬─────────────────────────────────┘
                       ↓
┌────────────────────────────────────────────────────────┐
│                  Update State                           │
│               (Application Layer)                       │
└──────────────────────┬─────────────────────────────────┘
                       ↓
┌────────────────────────────────────────────────────────┐
│                  Re-render UI                           │
│              (Presentation Layer)                       │
└────────────────────────────────────────────────────────┘
```

### 数据流示例：创建钱包

```rust
// 1. 用户点击"创建钱包"按钮
// Presentation Layer - create_wallet.rs
fn CreateWallet(cx: Scope) -> Element {
    let wallet_service = use_wallet_service(cx);
    
    let on_create = move |_| {
        // 2. 触发 Action
        wallet_service.create_wallet(
            name: "My Wallet",
            mnemonic_length: 12,
        );
    };
    
    render! {
        Button { onclick: on_create, "创建钱包" }
    }
}

// 3. 应用层处理
// Application Layer - wallet_service.rs
impl WalletService {
    pub async fn create_wallet(&self, name: &str, length: u8) -> Result<Wallet> {
        // 4. 调用领域层生成助记词
        let mnemonic = Mnemonic::generate(length)?;
        
        // 5. 调用加密服务
        let encrypted = self.crypto_adapter.encrypt(&mnemonic)?;
        
        // 6. 调用存储服务
        let wallet = self.wallet_repo.create(name, encrypted).await?;
        
        // 7. 更新状态
        self.state.set_active_wallet(wallet.clone());
        
        // 8. 发送事件
        self.events.emit(WalletCreated { wallet });
        
        Ok(wallet)
    }
}

// 领域层 - 业务规则
// Domain Layer - mnemonic.rs
impl Mnemonic {
    pub fn generate(length: u8) -> Result<Self> {
        if length != 12 && length != 24 {
            return Err(DomainError::InvalidMnemonicLength);
        }
        // 生成助记词...
    }
}

// 基础设施层 - 实际实现
// Infrastructure Layer - secure_storage.rs
impl WalletRepository for SecureStorageRepo {
    async fn create(&self, name: &str, encrypted: &str) -> Result<Wallet> {
        // 保存到 IndexedDB
        self.db.put("wallets", wallet).await?;
        Ok(wallet)
    }
}
```

---

## 🧩 模块设计

### 核心模块

#### 1. 认证模块 (Auth)
```rust
// 模块职责
- 用户注册/登录
- JWT Token 管理
- 会话管理
- 权限验证

// 主要组件
├── AuthService       // 认证服务
├── TokenManager      // Token 管理器
├── SessionStore      // 会话存储
└── AuthGuard         // 路由守卫
```

#### 2. 钱包模块 (Wallet)
```rust
// 模块职责
- 钱包创建/导入/删除
- 多链地址生成
- 钱包列表管理
- 钱包切换

// 主要组件
├── WalletService     // 钱包服务
├── AddressGenerator  // 地址生成器
├── WalletRepository  // 钱包仓储
└── WalletState       // 钱包状态
```

#### 3. 交易模块 (Transaction)
```rust
// 模块职责
- 交易构建
- 交易签名
- 交易发送
- 交易历史查询

// 主要组件
├── TransactionService  // 交易服务
├── TransactionBuilder  // 交易构建器
├── Signer             // 签名器
└── TxRepository       // 交易仓储
```

#### 4. 资产模块 (Asset)
```rust
// 模块职责
- 余额查询
- 代币管理
- NFT 管理
- 资产统计

// 主要组件
├── AssetService      // 资产服务
├── BalanceTracker    // 余额追踪器
├── TokenScanner      // 代币扫描器
└── NFTGallery        // NFT 画廊
```

#### 5. 加密模块 (Crypto)
```rust
// 模块职责
- 助记词生成/验证
- 密钥派生
- 数据加密/解密
- 签名/验签

// 主要组件
├── MnemonicGenerator // 助记词生成器
├── KeyDerivation     // 密钥派生
├── Encryptor         // 加密器
└── Signer            // 签名器
```

---

## 📊 状态管理

### 状态架构

```rust
// 全局状态结构
pub struct AppState {
    // 认证状态
    pub auth: Signal<AuthState>,
    
    // 钱包状态
    pub wallet: Signal<WalletState>,
    
    // 交易状态
    pub transaction: Signal<TransactionState>,
    
    // UI 状态
    pub ui: Signal<UiState>,
    
    // 配置
    pub config: Signal<AppConfig>,
}

// 认证状态
pub struct AuthState {
    pub user: Option<User>,
    pub token: Option<String>,
    pub is_authenticated: bool,
    pub session_expires_at: Option<DateTime>,
}

// 钱包状态
pub struct WalletState {
    pub wallets: Vec<Wallet>,
    pub active_wallet: Option<WalletId>,
    pub balances: HashMap<WalletId, Balance>,
    pub is_locked: bool,
}

// UI 状态
pub struct UiState {
    pub theme: Theme,
    pub language: Language,
    pub loading: bool,
    pub error: Option<ErrorMessage>,
    pub toast: Option<Toast>,
}
```

### 状态更新模式

```rust
// 使用 Signal 更新状态
use dioxus::prelude::*;

// 1. 创建 Signal
let wallet_state = use_signal(cx, WalletState::default);

// 2. 读取状态
let wallets = wallet_state.read().wallets.clone();

// 3. 更新状态（不可变）
wallet_state.write().wallets.push(new_wallet);

// 4. 批量更新
wallet_state.write().update(|state| {
    state.wallets.push(new_wallet);
    state.active_wallet = Some(new_wallet.id);
});

// 5. 派生状态（computed）
let active_wallet = use_memo(cx, |wallet_state| {
    wallet_state.read()
        .wallets
        .iter()
        .find(|w| w.id == wallet_state.read().active_wallet)
});
```

---

## 🔗 依赖关系

### 依赖规则

```
Presentation Layer
    ↓ (depends on)
Application Layer
    ↓ (depends on)
Domain Layer (Core - No Dependencies)
    ↑ (implements)
Infrastructure Layer
```

**原则**:
- ✅ 外层可以依赖内层
- ❌ 内层不能依赖外层
- ✅ 所有层都可以依赖领域层
- ✅ 基础设施层实现领域层接口

### 依赖注入

```rust
// 使用 Trait 定义接口
// Domain Layer
pub trait WalletRepository {
    async fn find_by_id(&self, id: &WalletId) -> Result<Option<Wallet>>;
    async fn save(&self, wallet: &Wallet) -> Result<()>;
}

// Infrastructure Layer - 实现接口
pub struct IndexedDBWalletRepo {
    db: IndexedDB,
}

impl WalletRepository for IndexedDBWalletRepo {
    async fn find_by_id(&self, id: &WalletId) -> Result<Option<Wallet>> {
        self.db.get("wallets", id).await
    }
}

// Application Layer - 依赖注入
pub struct WalletService {
    repo: Arc<dyn WalletRepository>,  // 依赖抽象
}

impl WalletService {
    pub fn new(repo: Arc<dyn WalletRepository>) -> Self {
        Self { repo }
    }
}

// Presentation Layer - 使用
let repo = Arc::new(IndexedDBWalletRepo::new(db));
let service = WalletService::new(repo);
```

---

## 🚀 部署架构

### 构建产物

```
dist/
├── index.html           # 入口 HTML
├── ironforge_*.wasm     # WASM 主文件
├── ironforge_*.js       # JS Glue Code
├── assets/
│   ├── styles.css       # 样式文件
│   ├── fonts/           # 字体文件
│   └── images/          # 图片资源
└── service-worker.js    # PWA Service Worker
```

### CDN 部署架构

```
┌────────────────────────────────────────────┐
│              Global CDN                     │
│  ┌──────────┬──────────┬──────────────┐   │
│  │   HTML   │   WASM   │   Assets     │   │
│  └──────────┴──────────┴──────────────┘   │
└────────────────────────────────────────────┘
              ↓ (Cache-Control)
┌────────────────────────────────────────────┐
│            Browser Cache                    │
│  ┌──────────┬──────────┬──────────────┐   │
│  │ IndexedDB│LocalStora│Memory Cache  │   │
│  └──────────┴──────────┴──────────────┘   │
└────────────────────────────────────────────┘
```

### 缓存策略

```rust
// Cache-Control Headers
HTML:  no-cache                    // 每次验证
WASM:  public, max-age=31536000   // 1 年 (内容哈希)
CSS:   public, max-age=2592000    // 30 天
Fonts: public, max-age=31536000   // 1 年
```

---

## 📈 可扩展性

### 插件化架构

```rust
// 定义插件接口
pub trait Plugin {
    fn name(&self) -> &str;
    fn version(&self) -> &str;
    fn init(&mut self, app: &mut App) -> Result<()>;
    fn destroy(&mut self) -> Result<()>;
}

// 插件管理器
pub struct PluginManager {
    plugins: Vec<Box<dyn Plugin>>,
}

impl PluginManager {
    pub fn register(&mut self, plugin: Box<dyn Plugin>) {
        self.plugins.push(plugin);
    }
    
    pub fn init_all(&mut self, app: &mut App) -> Result<()> {
        for plugin in &mut self.plugins {
            plugin.init(app)?;
        }
        Ok(())
    }
}

// 示例：NFT 插件
pub struct NFTPlugin;

impl Plugin for NFTPlugin {
    fn name(&self) -> &str { "NFT Gallery" }
    fn version(&self) -> &str { "1.0.0" }
    
    fn init(&mut self, app: &mut App) -> Result<()> {
        // 注册路由、组件、服务等
        app.router.add_route("/nft", nft_page);
        Ok(())
    }
}
```

---

## ✅ 总结

IronForge V2 的架构设计遵循以下核心原则：

1. **分层清晰** - 四层架构，职责明确
2. **依赖倒置** - 面向接口编程
3. **单向数据流** - 可预测的状态变化
4. **模块化** - 高内聚低耦合
5. **可测试** - 每层都可独立测试
6. **可扩展** - 插件化架构

这套架构将为项目提供坚实的技术基础，支撑长期发展。

---

**下一步**: 阅读 [技术栈选型](../02-technical-design/01-tech-stack.md)

**最后更新**: 2025-11-25
