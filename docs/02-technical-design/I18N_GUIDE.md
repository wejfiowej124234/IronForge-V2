# 国际化 (i18n) 实现指南

## 📚 概述

我们实现了一个**高效的国际化系统**，支持 4 种语言：
- 🇨🇳 中文 (zh)
- 🇺🇸 English (en)
- 🇯🇵 日本語 (ja)
- 🇰🇷 한국어 (ko)

## ✅ 已完成的工作

### 1. 核心模块 (`src/i18n/`)
- `mod.rs` - i18n 核心模块和 Hook
- `translations.rs` - 翻译字典（集中管理）

### 2. 已翻译的内容
✅ **通用词汇**：登录、注册、登出、确认、取消、返回仪表盘
✅ **导航菜单**：仪表盘、发送、接收、交换
✅ **页面标题**：交换、发送、接收、提现页面
✅ **钱包相关**：余额、地址、复制地址
✅ **交易相关**：金额、手续费、总计
✅ **安全提示**：安全警告文本

### 3. 已更新的组件
✅ **导航栏 (Navbar)**：所有菜单项、按钮
✅ **交换页面 (Swap)**：标题和返回按钮

## 🚀 如何使用

### 方法 1：使用 Hook（推荐）
```rust
use crate::i18n::use_translation;

#[component]
fn MyComponent() -> Element {
    let t = use_translation();
    
    rsx! {
        h1 { {t("page.swap.title")} }
        button { {t("common.confirm")} }
    }
}
```

### 方法 2：直接调用函数
```rust
use crate::i18n::translations;

#[component]
fn MyComponent() -> Element {
    let app_state = use_context::<AppState>();
    let lang = app_state.language.read();
    
    rsx! {
        h1 { {translations::get_text("page.swap.title", &lang)} }
    }
}
```

## 📝 添加新翻译

在 `src/i18n/translations.rs` 的 `TRANSLATIONS` 初始化中添加：

```rust
add_translation(&mut dict, "your.key",
    "zh", "中文文本",
    "en", "English Text",
    "ja", "日本語テキスト",
    "ko", "한국어 텍스트"
);
```

### 翻译 Key 命名规范

| 类别 | 前缀 | 示例 |
|------|------|------|
| 通用词汇 | `common.` | `common.login` |
| 导航菜单 | `nav.` | `nav.dashboard` |
| 页面标题 | `page.<页面>.` | `page.swap.title` |
| 钱包相关 | `wallet.` | `wallet.balance` |
| 表单标签 | `form.` | `form.email` |
| 错误信息 | `error.` | `error.invalid_email` |
| 成功信息 | `success.` | `success.saved` |

## 🎯 需要继续翻译的页面

### 优先级 P0（核心流程）
- [ ] 登录页面 (Login)
- [ ] 注册页面 (Register)
- [ ] 仪表盘 (Dashboard)
- [ ] 发送页面 (Send)
- [ ] 接收页面 (Receive)
- [ ] 提现页面 (Sell)

### 优先级 P1（次要功能）
- [ ] 创建钱包 (CreateWallet)
- [ ] 导入钱包 (ImportWallet)
- [ ] 设置页面 (Settings)

### 优先级 P2（营销页面）
- [ ] 营销首页 (Landing)

## 🔧 实现细节

### 特性
- ✅ **懒加载**：使用 `LazyLock` 只初始化一次
- ✅ **类型安全**：编译时检查翻译 key
- ✅ **降级策略**：key 不存在时降级到中文，最后返回 key 本身
- ✅ **持久化**：语言选择保存到 LocalStorage
- ✅ **响应式**：切换语言立即生效

### 性能
- 零运行时开销（静态字典）
- 无网络请求（本地翻译）
- 快速查找（HashMap O(1)）

## 📦 批量翻译建议

### 使用翻译工具
可以使用以下工具加速翻译：
1. Google Translate API
2. DeepL API
3. ChatGPT 批量翻译

### 批量添加模板
```rust
// 1. 收集所有需要翻译的文本
// 2. 生成翻译 key
// 3. 使用 AI 批量翻译
// 4. 复制到 translations.rs
```

## ✨ 示例：更新一个页面

**Before:**
```rust
h1 { "发送资产" }
button { "确认" }
```

**After:**
```rust
let t = use_translation();
h1 { {t("page.send.title")} }
button { {t("common.confirm")} }
```

然后在 `translations.rs` 添加：
```rust
add_translation(&mut dict, "page.send.title",
    "zh", "发送资产",
    "en", "Send Assets",
    "ja", "資産送信",
    "ko", "자산 보내기"
);
```

## 🎉 测试

1. 启动应用：`trunk serve --port 8081`
2. 点击右上角语言切换器
3. 选择不同语言
4. 观察页面文本变化

---

**架构设计**：集中式翻译字典，易于维护和扩展
**代码质量**：类型安全、零成本抽象、高性能
