# 后端服务集成指南（已弃用）

本文件过去包含对 Gas/费用/管理员系统等接口的手写说明，存在 `/api/*` 与路径/实现位置漂移风险。

## ✅ 当前权威做法

- OpenAPI：`GET http://localhost:8088/openapi.yaml`
- 业务接口前缀：`/api/v1/...`
- 统一响应：`{ code, message, data }`

## 非托管原则（必须遵守）

- 客户端签名；后端不接触私钥/助记词/密码。

> 需要补充“前端如何调用”的内容时，请从 OpenAPI 与实际代码实现中提炼，避免手写端点清单。
