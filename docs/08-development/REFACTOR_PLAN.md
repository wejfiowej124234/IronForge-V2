# IronForge UI 重构计划

> **日期**: 2025-11-27  
> **策略**: 保留业务逻辑，删除所有UI，重新设计  
> **设计风格**: 科技 + 苹果风格 + 质感

---

## 📋 执行计划

### 阶段 1: 代码分析与备份

#### ✅ 保留的业务逻辑代码

**核心业务逻辑（100%保留）**：
- `src/crypto/` - 加密、密钥管理、BIP39/BIP32/BIP44
- `src/blockchain/` - 区块链集成（BTC, ETH, SOL, TON）
- `src/services/` - 所有服务层（API调用）
  - `auth.rs` - 认证服务
  - `wallet.rs` - 钱包服务
  - `transaction.rs` - 交易服务
  - `balance.rs` - 余额服务
  - `token.rs` - 代币服务
  - 等等...
- `src/shared/` - 共享基础设施
  - `api.rs` - API客户端
  - `error.rs` - 错误处理
  - `state.rs` - 应用状态
  - `storage.rs` - 存储抽象
  - `cache.rs` - 缓存
  - `validation.rs` - 验证工具
- `src/features/wallet/state.rs` - 钱包状态管理
- `src/features/wallet/hooks.rs` - 钱包业务逻辑hooks
- `src/features/wallet/mod.rs` - 钱包模块导出

#### ❌ 删除的UI代码

**所有UI组件和页面**：
- `src/components/` - **全部删除**
  - `atoms/` - 原子组件
  - `molecules/` - 分子组件
  - `organisms/` - 有机体组件
  - `templates/` - 模板组件
  - `effects/` - 效果组件
- `src/features/*/pages/` - **全部删除**
  - `auth/pages/` - 登录、注册页面
  - `wallet/pages/` - 钱包相关页面
  - `landing/pages/` - 首页
  - `marketing/pages/` - 营销页面
  - 所有其他功能页面
- `src/main.rs` - **部分删除**
  - 保留：状态初始化、错误处理、生命周期
  - 删除：路由定义、页面渲染、UI相关代码

#### 📦 备份策略

创建 `src/archive/` 目录，将删除的代码移动到这里（不直接删除，方便回滚）

---

### 阶段 2: 设计系统建立

#### 设计风格定义

**核心风格**：
- **科技感**: 数字化、未来感、数据可视化
- **苹果风格**: 简洁、优雅、极简主义、毛玻璃效果
- **质感**: 深度、层次感、材质感、细腻光影

#### 设计Token系统

创建 `src/shared/design_tokens.rs`：

```rust
// 颜色系统
pub struct Colors {
    // 深色背景
    pub bg_primary: &'static str,      // #000000
    pub bg_secondary: &'static str,     // #1C1C1E
    pub bg_tertiary: &'static str,      // #2C2C2E
    
    // 科技蓝紫渐变
    pub tech_blue: &'static str,        // #007AFF
    pub tech_purple: &'static str,      // #5856D6
    pub tech_cyan: &'static str,         // #5AC8FA
    
    // 强调色
    pub accent_green: &'static str,      // #34C759
    pub accent_orange: &'static str,     // #FF9500
    pub accent_red: &'static str,        // #FF3B30
}

// 间距系统（8px基准）
pub struct Spacing {
    pub xs: &'static str,   // 4px
    pub sm: &'static str,   // 8px
    pub md: &'static str,   // 16px
    pub lg: &'static str,   // 24px
    pub xl: &'static str,   // 32px
    pub xxl: &'static str,  // 48px
}

// 圆角系统
pub struct Radius {
    pub sm: &'static str,   // 8px
    pub md: &'static str,    // 12px
    pub lg: &'static str,   // 16px
    pub xl: &'static str,   // 20px
    pub full: &'static str, // 9999px
}
```

#### 基础组件库

创建 `src/components/` 新结构：

```
components/
├── atoms/           # 原子组件（最小单元）
│   ├── button.rs
│   ├── input.rs
│   ├── card.rs
│   ├── icon.rs
│   └── ...
├── molecules/       # 分子组件（组合原子）
│   ├── form_field.rs
│   ├── wallet_card.rs
│   └── ...
├── organisms/       # 有机体组件（组合分子）
│   ├── header.rs
│   ├── navigation.rs
│   └── ...
└── templates/        # 页面模板
    ├── auth_layout.rs
    ├── wallet_layout.rs
    └── ...
```

---

### 阶段 3: 基于后端API设计页面

#### 后端API端点（从文档分析）

本阶段不再维护“手写端点清单”（容易与真实实现漂移）。

- 业务接口统一前缀：`/api/v1/...`
- 权威来源：后端 OpenAPI（`GET http://localhost:8088/openapi.yaml`）
- 统一响应结构：`{ code, message, data }`

提示：页面对接时优先覆盖 Send/Bridge 关键链路（Gas/Fees/Bridge Quote/Tx 记录），以 OpenAPI 中的真实路径与 schema 为准。

#### 页面结构设计

```
pages/
├── auth/
│   ├── login.rs          # 登录页面
│   ├── register.rs       # 注册页面
│   └── forgot_password.rs
├── wallet/
│   ├── create.rs         # 创建钱包（多步骤）
│   ├── import.rs         # 导入钱包
│   ├── list.rs           # 钱包列表
│   ├── detail.rs         # 钱包详情
│   ├── send.rs           # 发送交易
│   └── receive.rs        # 接收地址
├── dashboard/
│   └── home.rs           # 仪表盘首页
└── marketing/
    ├── landing.rs        # 营销首页（内容已确定）
    ├── card.rs           # 卡片页面（内容已确定）
    ├── earnings.rs      # 收益页面（内容已确定）
    └── airdrops.rs      # 空投页面（内容已确定）
```

---

### 阶段 4: 实施步骤

#### Step 1: 清理代码（1天）
1. 创建 `src/archive/` 目录
2. 移动所有UI代码到archive
3. 清理 `main.rs`，只保留业务逻辑
4. 更新 `Cargo.toml`，移除不需要的依赖

#### Step 2: 设计系统（3-4天）
1. 创建 `design_tokens.rs`（基于 DESIGN_SYSTEM_V3.md）
2. 实现基础原子组件（Button, Input, Card）
3. 实现毛玻璃效果组件（基础版 + 强化版）
4. 实现渐变和阴影系统
5. 实现光晕和发光效果
6. 区分营销页面和功能页面的组件变体

#### Step 3: 营销页面（2-3天）- 狠狠设计
1. 使用强化版设计系统（光晕、渐变、动画）
2. 实现 Hero 区域（大标题、3D 效果、粒子背景）
3. 实现功能展示卡片（强化毛玻璃、光晕效果）
4. 实现 CTA 按钮（大尺寸、渐变、光晕）
5. 添加动画和交互效果
6. 内容保持不变，UI 全面升级

#### Step 4: 核心流程（3-5天）- 干净整洁
1. 登录/注册页面（简洁、专业、友好）
2. 创建钱包流程（多步骤，清晰引导）
3. 导入钱包流程（简单、安全）
4. 使用基础版设计系统（干净整洁）

#### Step 5: 钱包功能（5-7天）- 优质服务
1. 钱包列表（清晰、易用）
2. 钱包详情（信息层次清晰）
3. 发送/接收（流畅、安全）
4. 交易历史（专业、易读）
5. 仪表盘（数据可视化、干净整洁）

---

## 🎯 设计规范参考

**新设计系统**: `docs-v2/05-ui-ux/DESIGN_SYSTEM_V3.md`

### 设计理念

- **苹果风格** + **未来科技** + **智能支付** + **眼镜支付** + **层次感** + **质感**
- **营销页面**: 视觉冲击力强，狠狠设计
- **功能页面**: 干净整洁，提供优质服务

### 关键设计元素

1. **专业颜色系统**：
   - 深空黑背景 (#0A0A0F)
   - 科技蓝紫渐变 (#6366F1 → #8B5CF6 → #06B6D4)
   - 智能支付绿 (#10B981)

2. **强化毛玻璃效果**（营销页面）：
   ```css
   background: rgba(26, 26, 36, 0.7);
   backdrop-filter: blur(30px) saturate(200%);
   box-shadow: 0 12px 48px rgba(0, 0, 0, 0.5),
               0 0 40px rgba(99, 102, 241, 0.1);
   ```

3. **基础毛玻璃效果**（功能页面）：
   ```css
   background: rgba(18, 18, 26, 0.6);
   backdrop-filter: blur(20px) saturate(180%);
   border: 1px solid rgba(255, 255, 255, 0.1);
   ```

4. **科技光晕效果**：
   ```css
   box-shadow: 
     0 4px 16px rgba(99, 102, 241, 0.3),
     0 8px 32px rgba(99, 102, 241, 0.2),
     0 0 40px rgba(99, 102, 241, 0.1);
   ```

---

## ✅ 验收标准

1. ✅ 所有业务逻辑代码保留且正常工作
2. ✅ 新的设计系统统一且美观
3. ✅ 所有页面使用新设计系统
4. ✅ 与后端API正确对接
5. ✅ 代码结构清晰，无重复
6. ✅ 营销页面内容保持不变，UI更新

---

## 📝 注意事项

1. **不要删除业务逻辑**：只删除UI相关代码
2. **保留状态管理**：`AppState`、`WalletState` 等
3. **保留API服务**：所有 `services/` 下的代码
4. **渐进式开发**：先做设计系统，再做页面
5. **测试驱动**：每完成一个功能就测试

---

**最后更新**: 2025-11-27

